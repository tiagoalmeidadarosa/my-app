import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "transparent",
    position: "absolute",
    bottom: 10,
  },
  button: {
    backgroundColor: "#000",
    padding: 16,
    borderRadius: 32,
  },
  icon: { color: "#FFF", fontSize: 32 },
});
